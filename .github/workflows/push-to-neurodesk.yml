name: push-to-neurodesk

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          repository: NeuroDesk/neurodesk
          token: ${{secrets.VNM_NEURODESK}}
          persist-credentials: false
          fetch-depth: 0
      - name: Setup git config
        run: |
          git config --local user.name "Github Action"
          git config --local user.email "actions@github.com"
          git config pull.ff only
      - name: Setup git remotes
        run: |
          git remote add subtree https://github-actions:${{secrets.BAR_SYNC}}@github.com/NeuroDesk/transparent-singularity.git master
          git remote add upstream https://github-actions:${{secrets.BAR_SYNC}}@github.com/NeuroDesk/neurodesk.git master
      - name: Pull transparent-singularity into subtree
        run:  git subtree pull --prefix neurodesk/transparent-singularity subtree master
      - name: Push to neurodesk
        run:  git push upstream master
